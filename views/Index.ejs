<!DOCTYPE html>
<html>
    <head>
        <script>
            function add(){
                location.href = "/board/add";
            }
            function info(no){
                location.href = "/board/info?no="+no;
            }
            function logout(){
                location.href = "/";
            }
        </script>
        <%- include("./head.ejs") %>
    </head>
    <body>
        <%- include("./nav.ejs") %>
        <div class="container" align="right" style="margin-top: 10px;margin-bottom: 10px;">
            <button onclick="logout()" class="btn btn-secondary">로그아웃</button>
        </div>
        <div class="container" style="background-image:url(./img/art1.jpg);background-repeat: no-repeat;background-size: cover;padding-bottom: 10px;">
            <table class = "table">
                <tr>
                    <th>No</th>
                    <th>Title</th>
                    <th>Date</th>
                    <th>Time</th>
                </tr>
                <%if(content.length == 0){%>
                    <tr>
                        <td colspan="4">해당 게시판은 게시글이 없습니다</td>
                    </tr>
                <%}else{
                    for(var i = 0; i < content.length; i++){%>
                        <tr onclick="info('<%=content[i].No%>')">
                            <td><%=content[i].No%></td>
                            <td><%=content[i].title%></td>
                            <td><%=content[i].date%></td>
                            <td><%=content[i].time%></td>
                        </tr>
                    <%}
                }%>
            </table>
        </div>
        <div class="container" align="right">
            <button onclick="add()" class="btn btn-primary" style="margin-top: 10px;">글쓰기</button>
        </div>
    </body>
</html>



<!-- sql -> [{},{},{},{}] -> length = 4 -->